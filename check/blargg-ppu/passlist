# ROMs that gate (regressions here fail the check).
check/blargg-ppu/data/01-vbl_basics.nes
check/blargg-ppu/data/04-nmi_control.nes
check/blargg-ppu/data/oam_read.nes
# Excluded, with reasons (see design: scanline granularity is deliberate):
#   02-vbl_set_time, 03-vbl_clear_time, 05-nmi_timing, 06-suppression,
#   07-nmi_on_timing, 08-nmi_off_timing - probe the vblank flag / NMI edge
#     at +-1 PPU-clock offsets; requires dot-level timing.
#   09-even_odd_frames, 10-even_odd_timing - odd-frame dot skip not modeled.
#   oam_stress - dot-level OAM access patterns during rendering.
#   ppu_open_bus - PPU open-bus decay not modeled.
#   mmc3-1..6 (mmc3_test_2) - all clock the IRQ counter via explicit A12
#     toggles ($2006 writes, rendering disabled); our MMC3 clocks per rendered
#     scanline (the approximation games like SMB3 use). Register-driven A12
#     clocking requires dot-level modeling - revisit if a game needs it.
